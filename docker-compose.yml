version: "3.2"

services:
  affinity:
    build: .
    ports:
      - "8080:80"
    volumes:
      - type: bind
        source: ./confd/target/.env
        target: /var/www/.env
        read_only: true
